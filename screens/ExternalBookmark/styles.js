import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 10,
    backgroundColor: "#fff",
  },
  cardContainer: {
    paddingTop: 5,
    paddingHorizontal: 5,
    paddingBottom: 60,
  },
});

export default styles;
