import { StyleSheet } from "react-native";

import { NAVBAR_DARK } from "../constants/colors";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: NAVBAR_DARK,
  },
});

export default styles;
